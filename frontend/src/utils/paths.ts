enum Paths {
  LOGIN = '/login',
  REGISTER = '/register',
  HOME = '/',
  PRODUCTS = '/products',
  POSTS = '/posts',
  USERS = '/users',
  ROLES = '/roles',
  PLAYGROUND = '/playground',
}

export default Paths;